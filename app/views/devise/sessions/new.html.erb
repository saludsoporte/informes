<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<%= image_tag('Logo_SALUD.png',size:"250x70") %>
 <h1 class="h3 mb-3 fw-normal">Porfavor Inicia Sesión</h1>
  <div class="form-floating">   
    <%= f.text_field :login, autofocus: true,class: :"form-control",id: :"floatingInput",placeholder: :"name@example.com"%> 
    <%= f.label :"Usuario o correo electrónico",for: :"floatingInput" %>
  </div>

  <div class="form-floating">      
      <%= f.password_field :password, autocomplete: "current-password",class: :"form-control",id: :"floatingPassword",placeholder: :"Password" %>
      <%= f.label :Contraseña,for: :"floatingPassword" %>      
  </div>
 
  <% if devise_mapping.rememberable? %>
    <div class="checkbox mb-3">
      <label>
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </label>
    </div>    
  <% end %>

  <div class="actions">     
    <%= f.submit "Iniciar Sesión", class: :"w-100 btn btn-lg btn-primary" %>
  </div>
  <%= render "devise/shared/links" %>
<% end %>


<%= javascript_pack_tag 'signin.js' %>

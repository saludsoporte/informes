<%= form_with(model: tabla_user_id) do |form| %>
  <% if tabla_user_id.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tabla_user_id.errors.count, "error") %> prohibited this tabla_user_id from being saved:</h2>
      <ul>
        <% tabla_user_id.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-floating">
    <%= form.text_field :user_id,value: :"#{params[:user].to_s}", class: :"form-control", placeholder: :"Login", hidden:true%>
  </div>
  
  <%= form.text_field :herramientum_id, value:Herramientum.find(params[:herramienta]).nombre_sistema,class: :"form-control", readonly:true %>      
  <div class="form-control">
    <%= form.label :id_user %>
    <%= form.text_field :id_user %>
  </div>  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

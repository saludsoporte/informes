<%= form_with(model: dato) do |form| %>
  <% if dato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dato.errors.count, "error") %> prohibited this dato from being saved:</h2>

      <ul>
        <% dato.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-floating field mb-3">    
    <%= form.text_field :nombre,id: :"nombre",class: :"form-control",placeholder: :"nombre" %>
    <%= form.label :nombre,for: :"nombre" %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar", class: :"btn btn-sm btn-primary" %>
  </div>
<% end %>

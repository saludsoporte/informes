<%= form_with(model: conexion_bd) do |form| %>
  <% if conexion_bd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conexion_bd.errors.count, "error") %> prohibited this conexion_bd from being saved:</h2>

      <ul>
        <% conexion_bd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: subdireccion) do |form| %>
  <% if subdireccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subdireccion.errors.count, "error") %> prohibited this subdireccion from being saved:</h2>

      <ul>
        <% subdireccion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
